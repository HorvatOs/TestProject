@model Vehicle.Models.DeleteMake

@{ ViewData["Title"] = "Delete"; }

<h4>Are you sure you want to delete this Vehicle Make?</h4>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Abrv)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Abrv)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div>
            <input type="submit" value="Delete" class="btn btn-danger"/>
            <a class="btn btn-outline-primary" style="margin-left:110px" asp-action="Index">Back</a>
        </div>
    </form>
</div>
